% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic-demo-data.R
\docType{data}
\name{synthetic}
\alias{synthetic}
\title{Synthetic Dataset for CHIPS Demo}
\format{
A list with elements \code{partitions}, \code{data}, and \code{means} as described.
}
\usage{
data(synthetic)
}
\description{
Synthetic Dataset for CHIPS Demo
}
\details{
A small synthetic dataset used in the CHIPS demo.

Load with \code{data("synthetic", package = "salso")} to get a list with
the following components:
\itemize{
\item \code{partitions}: integer matrix of size \verb{B x n} giving \code{B} sampled
clusterings (rows) of \code{n} items (columns), encoded with cluster labels.
\item \code{data}: numeric matrix of size \verb{n x 2} giving the two-dimensional
observations used to generate the partitions.
\item \code{means}: numeric array of size \verb{B x K x 2} giving cluster-specific means
across MCMC iterations (\code{B}), for each cluster (\code{K}) and dimension (2).
}

See also the CHIPS demo: \code{demo("chips-demo", package = "salso")}.
}
\examples{
data(synthetic)
str(synthetic)
}
\seealso{
chips
}
\keyword{datasets}
